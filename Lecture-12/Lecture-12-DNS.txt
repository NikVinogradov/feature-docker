kubectl gel all -n kube-system | greb dns
получим все инстансы для работы dns в кластере

coredns - основной dns сервер в кубере

nodelocaldns - локальный dns (кэшируюший)
он запускается как демонсет и соотв. запускает 1 на ноду

локального может и не быть

 
