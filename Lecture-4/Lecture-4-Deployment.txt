Деплоймент - родитель репликасета

strategy - способ обновления приложения
1) rolling Update - обновляет приложение по очереди
2) recreate

maxSurge - на какое количество реплик мы можем увеличить по сравнению с replicas
maxnaveliable - на сколько меньше
такде в эти поля можно писать проценты
max Surge: 10%

kubectl set image deployment my-deployment '*=nginx:1.13'
ты втыкаешь имедж в Деплоймент

kubectl rollout undo deployment my-deployment
это откатывание изменений

что если контейнер поднялся, но приложение в нем не стартануло
в контексте кубера мы должны использовать пробы, чтобы
убедиться а живо ли приложение, это своего рода хелсчекеры

Probes:
1) Liveness Prode - чекер
2) Readness Probes - готово ли принимать трафик, если нет, то убирается из балансировки

failureThrechold - соклько раз может провалить проба
successs.. - достаточно 1 успешной пробы чтобы считать что приложение работает норм

initialDelaySecond - первую пробу нужно после столько -то секунд сделать

cpu: 50m

1 cpu = 1000 mcpu
1cpu - это все процессорное время

kubectl describe node
это инфа по ноде


