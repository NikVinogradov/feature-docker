servcie - набор правил для роутинга и ничего более
1) IP Tables
2) ipvc
kubectl get pod --namespace=mynamespace -0 wide

сервис не пингуется так как он работает на tcp
myservice.mynamespace.svc.cluster.local

Network:
Цель - под одной ноды, может обратиться на под любой другой ноды
как бы сеть не была сконфигурирована

Сетевой плагин с минимальным оверхедом
Flannel
ограничение - если закончатся подсети, то новую подсеть просто так не добавить
Calico - второй плагин, более функциональный

можно более гибко управлять сетям используя Network Policy
там можно настроить рули, которые говорят, а кто с кем может общаться
и кто с кем не может общаться

обычно трафик перебрасывается из ingress сразу в ip подов

отказоустойчивый кластер
3 мастера
взаимодействие с нодами через nginx-proxy

iptables -t nat -S | grep my-deployment